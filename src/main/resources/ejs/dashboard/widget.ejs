<div class="widget" data-name="<%= widget.name %>">
	<div class="box">
		<% if (obj.widget && widget.configuration.title) { %>
			<div class="title"><%= widget.configuration.title %></div>
		<% } %>
		<% if (obj.errors) { %>
		<div class="errors">
			<ul>
				<% for (var idx in errors) { %>
				<li><%= errors[key] %></li>
				<% } %>
			</ul>
		</div>
		<% } %>
	</div>
</div>